import { GoodsType } from "./types";

export const taxes = {
  imported: 5,
  basic: 10,
};

export const goods: GoodsType = {
  Books: ["book"],
  Food: ["meat", "chocolate", "chocolates", "water"],
  Medical: ["pills"],
};
